# Plan 08-02 — AudioService

**Phase:** 8 — Polish & Animations
**Wave:** 2
**Depends on:** 08-01
**Goal:** Singleton AudioService через Riverpod. 3 звука: quest_complete, level_up, ui_tap.

## Files to Read Before Starting

- `levl/lib/core/supabase/isar_service.dart` — паттерн Riverpod singleton

## Steps

### 1. Добавить звуковые файлы

Скачать короткие MP3 (< 1 сек):
- `levl/assets/sounds/quest_complete.mp3` — короткий позитивный тон
- `levl/assets/sounds/level_up.mp3` — насыщенный звук достижения
- `levl/assets/sounds/ui_tap.mp3` — лёгкий клик

Бесплатно: mixkit.co/free-sound-effects или freesound.org

### 2. Создать AudioService

`levl/lib/core/audio/audio_service.dart`:

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:just_audio/just_audio.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';

part 'audio_service.g.dart';

class AudioService {
  final AudioPlayer _player = AudioPlayer();

  Future<void> playQuestComplete() => _play('assets/sounds/quest_complete.mp3');
  Future<void> playLevelUp()       => _play('assets/sounds/level_up.mp3');
  Future<void> playUiTap()         => _play('assets/sounds/ui_tap.mp3');

  Future<void> _play(String asset) async {
    try {
      await _player.setAsset(asset);
      await _player.seek(Duration.zero);
      await _player.play();
    } catch (_) {}
  }

  void dispose() => _player.dispose();
}

@Riverpod(keepAlive: true)
AudioService audioService(Ref ref) {
  final service = AudioService();
  ref.onDispose(service.dispose);
  return service;
}
```

### 3. Кодогенерация

```bash
cd levl && dart run build_runner build --delete-conflicting-outputs
```

## Acceptance Criteria

- `levl/lib/core/audio/audio_service.dart` содержит `class AudioService`
- `levl/lib/core/audio/audio_service.g.dart` сгенерирован
- `levl/assets/sounds/` содержит 3 аудио файла
